[pytest]
testpaths = tests
addopts = -q -m "not network"
markers =
    network: hits real external services (MLB Stats API); excluded by default, run with -m network
filterwarnings =
    ignore::DeprecationWarning
